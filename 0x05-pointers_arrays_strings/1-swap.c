#include "main.h"

/**
 * swap_int -> give that two integers swap the values they are holding
 * @a: parameter 1
 * @b parameter 2
 */
void swap_int(int *a, int *b)
{
	int tmp;

	a = 47, b = 68
		tmp = *a;
		*a = *b;
		b* = tmp;
}
